import 'package:flutter/material.dart';

class userPatient extends StatelessWidget {
  const userPatient({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
